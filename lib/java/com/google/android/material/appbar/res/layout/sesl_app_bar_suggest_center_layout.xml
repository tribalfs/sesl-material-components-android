<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/suggest_app_bar_center_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_centerInParent="true"
    android:layout_marginHorizontal="@dimen/sesl_appbar_viewpager_item_margin_horizontal"
    android:background="@android:color/transparent"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingBottom="@dimen/sesl_appbar_viewpager_item_margin_vertical_for_light_effect">

    <ImageView
        android:id="@+id/suggest_app_bar_top_image"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/suggest_app_bar_title"
        style="@style/TextAppearance.Design.CollapsingToolbar.Suggest.Inverse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="center"
        android:lineSpacingExtra="-2dp"
        android:lineSpacingMultiplier="1"
        android:maxLines="3"
        android:visibility="gone" />

    <TextView
        android:id="@+id/suggest_app_bar_sub_title"
        style="@style/TextAppearance.Design.CollapsingToolbar.Suggest.SubTitle.Inverse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="center"
        android:lineSpacingMultiplier="1"
        android:maxLines="2"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/suggest_app_bar_bottom_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/sesl_appbar_suggest_button_top_margin"
        android:clipChildren="false"
        android:gravity="top|center_horizontal"
        android:orientation="horizontal" />
</LinearLayout>
